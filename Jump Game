class Solution:
    def canJump(self, nums: List[int]) -> bool:
        n=len(nums)
        maxIndex=0
        for index in range(0,n):
            if(index>maxIndex):
                return False
            maxIndex=max(maxIndex,index+nums[index])
        return True
