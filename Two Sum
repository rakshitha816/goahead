class Solution(object):
    def twoSum(self, nums, target):
        d={}
        for a in range(0,len(nums)):
            b=target-nums[a]
            if(b in d):
                return[a,d[b]]
            d[nums[a]]=a
        
